{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build C++ Debug",
      "type": "shell",
      "command": "cmake -G \"Unix Makefiles\" -S src/cpp -B build -DCMAKE_BUILD_TYPE=Debug && make -j $(nproc) -C build"
    }
  ]
}
